/**
 * 权重动画组
 * 1、目的：多个权重动画组之间的权重合并，比如：过度动画（从走到跑等）
 * 2、要求：
 *    A、动画组中的动画必须相同
 *    B、如果是骨骼动画，要求骨骼必须相同。
 */
import { AnimationGroup } from "./animationGroup";

/**todo */
export class WeightMixAnimationGroup  {


}